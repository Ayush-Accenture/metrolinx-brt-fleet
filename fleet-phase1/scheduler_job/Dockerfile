# Generated by GitHub Copilot
# Minimal Azure CLI image – curl is pre-installed in the azure-cli image.
# No application code is installed; the container only ships trigger.sh.

FROM mcr.microsoft.com/azure-cli:latest

# ── Copy the trigger script into the image ──────────────────────────────────
COPY trigger.sh /trigger.sh

# Make the script executable
RUN chmod +x /trigger.sh

# Default command – executed by the Container Apps Job on each run
CMD ["/trigger.sh"]
